# Nuxt dev/build outputs
.output
.nuxt
.nitro
.cache
dist

# Environment variables
.env
.env.*
!.env.example

# Node dependencies
node_modules

# Package Manager Locks (Optional: remove bun.lockb if you don't commit it, but usually lockfiles ARE committed)
# Note: Usually lockfiles like bun.lockb are committed to github to ensure reproducible builds.
# If you want to ignore them, uncomment the following line:
# bun.lockb

# Agent / IDE / Editors
.agent
.gemini
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*
